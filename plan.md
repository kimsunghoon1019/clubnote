# ClubNote 반응형 — 제품 수준

한 제품, 한 주소, 폭마다 **다른 구성**. 데스크톱을 줄인 버전이 아니라, 390에서도 운영진이 손가락으로 출석을 찍고 회원·회계·일정을 끝낼 수 있는 콘솔이다.

참고 밀도: Toss 증권 앱(좁을 때) + Toss 증권 웹(넓을 때). 로고·상표는 복제하지 않는다. 흰 화면, `#3182F6`, 상승 빨강 `#f04452`, Pretendard.

## 품질 기준 (이하면 미완성)

- 390에서 가로 스크롤로 본문이 새지 않는다. 표만 예외적으로 가로로 밀리지 않는다. 좁은 화면의 기본 정보는 **리스트**다.
- 호버로만 되는 조작이 없다. 마우스 전용(드래그, 더블클릭, 우클릭)은 터치 대응이 있다.
- 첫 페인트에 데스크톱 헤더가 폰에 깜빡이지 않는다. 분기는 CSS(`lg:`)가 기본이다.
- 상세는 레일 내용을 시트에 그대로 구겨 넣지 않는다. 폰에서는 **전체화면 상세**(자체 헤더·저장·닫기).
- 1440에서 지금 콘솔이 깨지지 않는다. 헤더 탭, 300px 레일, 캘린더 드래그, 출석 표.
- 검증은 스크린샷 한 장이 아니다. 390 · 768 · 1440에서 로그인부터 저장까지 손으로 탄 것과 같은 경로.

## 구성 두 개, 코드는 하나

라우트는 그대로 `/` `/members` `/attendance` `/finance` `/calendar` `/mypage` `/login`.

| | Compact `< 1024` | Console `≥ 1024` |
|---|---|---|
| 이동 | 하단 탭 5 + 헤더 아바타 | 헤더 탭 5 + 이름 칩 |
| 목록 | 아바타 행, 큰 숫자, 칩 필터 | `DataTable` |
| 상세 | 전체화면 (`DetailScreen`) | 우측 레일 300px |
| 캘린더 | 월간 + 아래 당일 의제 | 월간 + 우측 패널 |
| 제스처 | 탭, 롱프레스, 스와이프 닫기 | 클릭, 드래그, 더블클릭, 우클릭 |
| 하단 | 탭 + safe-area. 상태바 없음 | `StatusBar` |

768은 Compact를 쓰되 여백·2열·큰 시트를 허용한다. “큰 폰”으로 방치하지 않는다.

구현은 페이지를 두 벌 복사하지 않는다. **목록 슬롯 + 상세 슬롯**이 폭에 따라 테이블/리스트, 레일/전체화면을 고른다.

```
MembersView
  ├─ RosterList    (기본, lg:hidden)
  ├─ DataTable     (hidden lg:block)
  └─ DetailSurface → Compact면 DetailScreen, Console이면 RightRail
```

## 디자인 토큰 (코드에 박는다)

`globals.css` / `tailwind.config.ts`:

- 폭: `md 768`, `lg 1024`, `xl 1440`. 레이아웃 분기는 **`lg` 하나**만. `md`는 여백·열 수.
- 높이: `100dvh`. `h-screen` 제거.
- 터치: 아이콘 버튼 44×44, 리스트 행 최소 56. Console 표 행은 지금 높이 유지.
- 타입: Compact 본문 16 / 제목 20 / 캡션 13. Console은 지금 14 / 18 / 12.
- 입력 Compact 16px (iOS 줌 금지).
- 모션: 시트 280ms, 저장 펄스는 기존 `save-press`. `prefers-reduced-motion: reduce`면 0.
- `env(safe-area-inset-*)` 헤더·탭·전체화면 닫기 버튼.

`layout.tsx` viewport: `width=device-width, initial-scale=1, viewport-fit=cover`. `theme-color #ffffff`.

## 셸

**Compact 헤더** — 로고, 검색 아이콘, 알림, 아바타. 저장 중/실패는 알림 옆 12px 점(파랑/빨강). 이름 문자열 없음.

**하단 탭** — 홈 · 회원 · 출석 · 회계 · 캘린더. 아이콘+라벨, 활성은 굵은 잉크 + 브랜드가 아님(토스 탭처럼 절제). 현재 라우트와 맞춤. safe-area 포함 높이 ~56+inset.

**Console 헤더/상태바** — 지금 픽셀을 유지. `hidden lg:flex` / `lg:hidden`으로만 전환.

**검색** — Compact는 기존 `SearchOverlay` 전면. 결과 행도 44px.

**알림** — Compact는 우측 패널 대신 전면 리스트.

## 상세 서피스

새 컴포넌트 `DetailSurface`.

- Console: 기존 `RightRail`.
- Compact: `DetailScreen` 전체화면, `translate-y` + dim. 헤더 `[닫기] 제목 [저장]`. 포커스 트랩, ESC·뒤로가기·스와이프 다운으로 닫기. 스크롤은 헤더 아래만. 열리면 뒤 목록 `inert` + 탭 바 숨김(저장과 탭이 안 싸운다).

레일 JSX를 통째로 넣지 않는다. `MemberRail` / `TransactionRail` / 캘린더 패널은 패딩만 반응형으로 두고, Compact 헤더는 `DetailScreen`이 담당해서 레일 안의 작은 닫기를 중복하지 않는다.

## 페이지 구성 (Compact가 새로 디자인하는 부분)

### 로그인
가운데 카드 유지. 키보드가 버튼을 가리지 않게 `scroll-padding`. 에러는 필드 아래 한 줄.

### 홈
세로 리듬: 인사/시계 → 오늘 멘트 → KPI 2×3(큰 숫자, 스파크는 `xl`만) → 다음 연습 카드 → 참여 바 차트(높이 120, 탭하면 그날 참여자 시트) → 분류 파이. 우측 미니캘린더는 Compact에 없다. 다음 연습 카드가 그 역할을 한다. Console은 지금 6열+레일.

### 회원관리 — Compact 리스트
행: 아바타 · 이름 · 직책 필 · 분류 · 성실도 얇은 바. 탭 → 회원 전체화면. 상단 검색 필드 + 분류 칩은 한 줄(넘치면 가로 스크롤, 글자 줄바꿈 없음). 비활동은 접힌 섹션. 추가 버튼은 우측 하단 FAB가 아니라 헤더 `+`(탭 바와 충돌 방지). Console 표·헤더 분류 추가는 유지.

### 출석체크 — Compact의 핵심 화면
연습실에서 한 손으로 쓴다.

- 상단: 큰 날짜(요일·시간) , 좌우로 연습 이동, `출석표` 텍스트 버튼.
- 요약 한 줄: `출석 n · 미체크 n`.
- 분류 칩.
- 명단 행: 아바타·이름·분류, 오른쪽 **세그먼트 출석/지각/결석** (높이 44, 지금 `AttendanceToggle`을 Compact 크기로).
- 저장은 명단 아래 작은 버튼. 하단 탭은 유지하고, 이전화면으로 돌아가는 `뒤로`를 둔다.
- 행 본문 탭 → 회원 전체화면. 토글은 전파 중지.
- Console: 왼쪽 날짜 레일 + `min-w-[920px]` 표 유지.

### 회계
Compact: 잔액 히어로, 수입/지출 두 칸, 기간 세그먼트, 거래 행(날짜·적요·금액 빨강/파랑). 탭 → 증빙 포함 전체화면. 필터·내보내기는 헤더 `…`. Console: 지금 표+레일.

### 캘린더
Compact: 월 전폭. 날짜 숫자+신촌 날씨(아이콘·최고기온)+점(연습은 브랜드). 날짜 탭 → 카드 오버레이(요일·날씨·의제). `+`는 툴바. 일정 탭 → 전체화면 편집. 빈 날짜 롱프레스 → 생성. 드래그 없음.

Console: 지금 월간+우측 패널, `pointer: fine` 드래그·더블클릭·컨텍스트 메뉴 유지. Compact에선 그 핸들러를 붙이지 않는다.

### 마이페이지
Compact 1열, 사진 크게, 저장 스티키. Console 2열+레일.

## 제스처·포인터

`pointer: coarse` / `fine`은 캘린더·리사이즈 컬럼에만. 셸 분기는 뷰포트.

| Console | Compact |
|---|---|
| 더블클릭 생성 | 롱프레스 또는 `+` |
| 드래그 이동 | 상세에서 날짜 변경 |
| 우클릭 삭제 | 상세 하단 위험 버튼 / 롱프레스 메뉴 |
| 컬럼 리사이즈 | 없음 |
| hover 행 | 없음. 선택 링만 |

## 엔지니어링 규칙

- **CSS-first 분기.** `window.innerWidth`로 탭 바를 마운트하지 않는다. 필요하면 `matchMedia`는 제스처 활성에만.
- **한 상세, 두 프레임.** 레일/전체화면이 각각 다른 폼 상태를 갖지 않게 `DetailSurface` 한 트리.
- **기존 표 로직 재사용.** 정렬·선택·출석 저장·증빙 업로드는 store API 그대로. Compact는 렌더만 다르다.
- **포커스.** `DetailScreen`·모달 포커스 트랩. 닫으면 열었던 행으로 복귀.
- **스크롤.** Compact 본문 하나. `h-screen overflow-hidden` 중첩 스크롤을 Compact에서 없앤다. Console은 지금처럼 페인이 나뉜다.
- **파일.** 증빙·프로필은 `accept="image/*,application/pdf"` 유지. Compact 미리보기는 가로 100%.
- **회귀.** 새 Compact 마크업이 1440 표의 `data-` 셀렉터를 가리지 않게 Console 테이블을 조건 렌더.

만지지 않는 것: API, Supabase, R2, 세션, 성실도/참여도 공식, SMS 목업.

## 구현 순서 (창 하나 = 커밋 하나)

기초가 품질을 고정한다. 페이지는 그 위에 “Compact 구성 완성”으로만 올린다. 가로 스크롤 임시방은 커밋하지 않는다.

| # | 목표 | 완료 조건 |
|---|---|---|
| 1 | 토큰 + 셸 | 390에서 하단 탭·짧은 헤더, 1440에서 지금 헤더·상태바. 새로고침 시 셸 깜빡임 없음. safe-area, `dvh`, viewport. |
| 2 | `DetailSurface` | 회원 행을 390에서 전체화면으로 열고 닫고, 1440에서 레일. 포커스·뒤로가기·inert. 홈/회원/출석/회계/마이에 연결. |
| 3 | 출석 Compact | 리스트+큰 토글+스티키 저장으로 출석을 끝까지 찍는다. 1440 표·날짜 레일 유지. |
| 4 | 회원 Compact | 리스트·칩·전체화면 편집. 분류/직책 추가는 Console 헤더 유지, Compact는 `+`/메뉴. |
| 5 | 회계 Compact | 히어로+거래 리스트+증빙 전체화면. 내보내기는 `…`. |
| 6 | 캘린더 Compact | 월간+의제+롱프레스 생성. Console 드래그 유지. |
| 7 | 홈·로그인·마이·모달 | Compact 홈 리듬, 모달 폭, 입력 16px. `AGENTS.md`에 폭 규칙·두 구성을 적고 `데스크톱(~1440px)`만의 문장을 교체. |

3번이 연습실 품질의 판정이다. 1–2 없이 페이지를 시작하지 않는다.

## 검증 매트릭스

매 커밋, 해당 페이지:

| 폭 | 볼 것 |
|---|---|
| 1440×900 | 기존 `tmp/verify-*.mjs`. 레일, 표, 캘린더 드래그, 헤더 탭. |
| 768×1024 | Compact 셸, 리스트가 두 열 여백을 쓰는지, 상세가 전체화면인지. |
| 390×844 | 가로 넘침 없음(`document.scrollWidth === innerWidth`). 탭 44px. 로그인→해당 기능 저장→뒤로 목록. |

브라우저: 데스크톱 Chrome, iOS Safari 폭(Playwright `iPhone 14`). 레이아웃 커밋은 390과 1440 스크린샷을 `screenshots/`에 남긴다.

## 하지 않는 것

- `/m` 서브트리, PWA, 푸시, 단원용 앱
- Compact에서 920px 표를 가로로 밀게 두고 끝내는 일
- 1440 헤더 탭 제거, 레일 폭 변경, 브랜드 색 변경
- 캘린더를 리스트만 있는 화면으로 바꾸기 (월간은 유지)
- 다크 모드
